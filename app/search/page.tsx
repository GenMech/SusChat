import SearchPageContainer from "@/components/main/SearchPageContainer";

export default function SearchPage() {
  return (
    <main className="relative flex flex-col items-center justify-center h-full w-full m-auto">
      <SearchPageContainer />
    </main>
  );
}
